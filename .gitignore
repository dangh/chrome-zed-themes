__pycache__/
.zed-themes/
tools/chrome-prof/

# dist/ holds the generated themes and is committed; ~/.gitignore excludes "dist"
!dist/

# Chrome drops this into whichever theme folder is loaded unpacked
Cached Theme.pak

# zips/ are for the one-time manual uploads that create each store item
zips/
